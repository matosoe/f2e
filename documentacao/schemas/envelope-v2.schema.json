{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://f2e.local/schemas/envelope-v2.schema.json",
  "title": "F2E Envelope v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["metadata", "source", "processing", "data"],
  "properties": {
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "required": ["eventId", "sourceRecordId", "schema", "format", "createdAt"],
      "properties": {
        "eventId": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
        "sourceRecordId": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
        "schema": {
          "type": "object",
          "additionalProperties": false,
          "required": ["id", "version"],
          "properties": {
            "id": { "type": "string", "minLength": 1 },
            "version": { "type": "string", "minLength": 1 }
          }
        },
        "format": { "type": "string", "minLength": 1 },
        "createdAt": { "type": "string", "format": "date-time" },
        "transactionId": { "type": "string" },
        "correlationId": { "type": "string" },
        "traceId": { "type": "string" }
      }
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "bucket", "key", "etag", "fileFormat", "fileSize"],
      "properties": {
        "type": { "const": "s3" },
        "system": { "type": "string" },
        "bucket": { "type": "string", "minLength": 1 },
        "key": { "type": "string", "minLength": 1 },
        "versionId": { "type": "string" },
        "etag": { "type": "string", "minLength": 1 },
        "fileName": { "type": "string" },
        "fileFormat": { "type": "string" },
        "fileSize": { "type": "integer", "minimum": 0 }
      }
    },
    "processing": {
      "type": "object",
      "additionalProperties": false,
      "required": ["jobId", "chunkId"],
      "properties": {
        "jobId": { "type": "string", "minLength": 1 },
        "chunkId": { "type": "string", "minLength": 1 },
        "recordNumber": { "type": "integer", "minimum": 1 },
        "byteOffset": { "type": "integer", "minimum": 0 },
        "byteLength": { "type": "integer", "minimum": 1 }
      }
    },
    "data": {}
  }
}
